apiVersion: v1
kind: Service
metadata:
  name: database-service
  labels:
    app: database-pod
spec:
  type: ClusterIP
  selector:
    app: database-pod
  ports:
    - protocol: TCP
      port: 3306
      targetPort: 3306
